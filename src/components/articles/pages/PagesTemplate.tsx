/**
 * every content in pages folder must import this module.
 * has basic template for each page.
 */

/**
 * Each page must explicitly connect this interface to the declaration.
 */
export default interface TypeArticleProps {
  name: string;
}
